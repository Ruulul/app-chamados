import { Card, Typography } from "@mui/material";

const Home = (props) => {
  return (
    <Card elevation="3" sx={{ padding: 5, width: "100%" }}>
      <Typography variant="h2">Home</Typography>
    </Card>
  );
};

export default Home;
